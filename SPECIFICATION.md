# Somos Venezuela（ソモス・ベネズエラ）統合仕様書 v0.1
（理念 → 要件 → 画面 → データ → バッチ → 運用 → テスト → OSS運用）

---

## 0. プロジェクト概要

### 0.1 プロジェクト名
**Somos Venezuela（ソモス・ベネズエラ）**
「私たちはベネズエラ（＝運命共同体）」という名を冠した、ベネズエラの復興を世界中のチームで支えるための **オープンソース・寄付プラットフォーム**。

### 0.2 ビジョン・ステートメント（最初の一言）
> **あなたの関心が、ベネズエラへの架け橋になります。**
> ひとりの力では変えられない現状も、
> 世界中から集まる「一歩」が重なれば、大きな変化を生み出します。
> **ベネズエラの未来を、私たちと一緒に耕しませんか。**

---

## 1. フィロソフィー（価値観・支援の姿勢）

### 1.1 支援の在り方：Team Up, Not Help
「助けてあげる」という上下関係ではなく、同じ地球に住む **ひとつのチーム**として関わる。
一方的な施しではなく、共鳴と連帯にもとづく支援を目指す。

### 1.2 目的：復興への土壌づくり
今苦しんでいる人への直接支援だけでなく、国外へ脱出した人々が
「いつか戻って復興に力を貸したい」と思えるような **希望ある国の状態を取り戻す**土壌を耕す。

### 1.3 参加のハードル：無理のない関心
「多額の寄付」だけが正解ではない。
まずは **現状を知り、関心を持ち続けること**。
そして各人が **無理のない範囲**で一歩を踏み出せる状態を尊重する。

---

## 2. スコープ・フェーズ・北極星（KGI）

### 2.1 対象スコープ
- 対象国：**ベネズエラのみ**
- 将来拡張：他国対応は行わない（必要なら別Webサイトとして構築）

### 2.2 展開フェーズ
- **Phase 1（Now）**：日本語でローンチ。日本のユーザーがベネズエラの現状を身近に感じ、支援に踏み出せる状態をつくる。
- **Phase 2**：グローバル展開（英語→スペイン語→話者数の多い順）

※Phase 1では **YouTubeカードは日本語サイトのみ**。

### 2.3 北極星KGI（Phase 1から寄付金額）
- **KGI：寄付実行金額（Donation Amount）**
  - "Somes Venezuelaを起点として"行われた寄付の金額（後述の定義に従う）

#### 2.3.1 KGIの定義（重要）
本システムは資金を預からない（集金しない）。
したがって、KGIは以下のいずれかで計測する（Phase 1から必須）：

- **方式A（推奨）**：寄付先団体または寄付基盤が提供する **キャンペーン/ファンドレイズページ**の「公開合計金額」をKGIとして採用
  - 例：キャンペーンページに "Total donated" 等が表示される
  - システムが定期取得し、合計を集計してKGI表示する
- **方式B**：寄付基盤が提供する **API**で合計金額を取得しKGIとして採用
- **方式C（不可）**：単なる外部寄付フォームへのリンクのみ（合計金額が取得不能）
  - Phase 1では採用しない（KGI要件を満たせないため）

> Phase 1では「寄付金額が取得できる寄付導線（キャンペーンページ等）」のみを掲載可能とする。

---

## 3. プロダクトの約束（編集方針・信頼の作り方）

### 3.1 中立性：現実を淡々と、丁寧に
- 生活・人道・医療・教育・移動・難民/移民など「人の暮らし」に関わる事実を中心に扱う
- 政治的主張、断罪、扇動、煽りは行わない
- 外交・制裁など国際政治の駆け引きは **原則扱わない**
  - 例外：人道・生活への影響に直接関係する "事実" のみ、必要最小限で記述（評価語なし）

### 3.2 事実 / 推測 / 未確認を明確に分ける
AIが作成する記事は、読者が「どれが確かな情報か」を一目で判断できるようにする。

- **Fact（事実）**：出典URL必須
- **Inference（推測）**：推測であることを明示し、根拠となる事実と分離
- **Unverified（未確認）**：未確認の理由を添え、推測で埋めない

#### 3.2.1 表示仕様（視覚で分かる）
- **Fact：緑**（ラベル「事実」必須）
- **Inference：黄**（ラベル「推測」必須）
- **Unverified：赤**（ラベル「未確認」必須）

※色覚多様性のため、色だけに依存せず **必ずテキストラベルを併用**する。

### 3.3 尊厳の保護（センセーショナルにしない）
- 個人の特定につながる描写、差別/侮蔑、過度な悲惨演出は禁止
- 「理解を助ける」ことに焦点を置く（ショックの消費をしない）

---

## 4. システム全体像

### 4.1 プラットフォーム
- デプロイ先：Railway
- 予定ドメイン：`somos-venezuela.up.railway.app`
- Open Source：GitHub リポジトリ `somos-venezuela`

### 4.2 構成（論理）
- Public Web（公開サイト）
- Admin Web（管理画面）
- Worker（収集・生成・レビュー・公開・KGI取得バッチ）
- DB（PostgreSQL推奨）

---

## 5. 機能要件（Public）

### 5.1 公開ページ一覧
- **Home**
  - 最新記事への導線
  - "30秒でわかる"の入口
  - 寄付KGIの表示（累計寄付金額）
  - 寄付導線（キャンペーン一覧）
- **Daily記事一覧**
  - 日付/タイトル/要点、フィルタ（直近◯日）
- **Daily記事詳細**
  - 記事本文（Fact/推測/未確認の色分け表示）
  - 出典リンク一覧
  - 寄付導線（目的別）
  - YouTubeカード（日本語のみ）
- **History（歴史）**
  - 簡易年表（年/出来事/説明/出典URL）
  - 年表に沿った説明文（中立・事実中心）
- **Policy / About**
  - フィロソフィー、編集方針、事実/推測/未確認の定義、訂正方針、免責
- **Sources**
  - 採用ホワイトリストの公開（透明性）

### 5.2 YouTubeカード（日本語のみ・手動）
- 1カードに含める項目
  - サムネイル
  - タイトル / チャンネル名
  - 動画内容の説明（100〜200字）
  - おすすめ理由（箇条書き2〜3）
  - 注意書き（例：本サイトが動画主張を支持するものではない）
  - 外部リンク
- 表示箇所：Daily記事詳細の下部（もしくはサイド）
- 登録方法：管理画面から手動

---

## 6. 機能要件（Admin）

### 6.1 管理画面の目的
- ホワイトリストや寄付キャンペーンの管理、記事生成・レビューの監視、プロンプト/テンプレ更新を安全に行う

### 6.2 管理画面一覧（MVP必須）
- **Admin Dashboard**
  - KGI（寄付金額）の推移、当日/週/月のサマリ
  - 直近のバッチ結果（成功/失敗/保留）
  - 重大エラー通知
- **Sources（ホワイトリスト管理）**
  - ソースCRUD（RSS/スクレイプURL、カテゴリ、言語、取得方法、ON/OFF）
  - 失敗率/不合格率の統計表示
- **Articles**
  - 記事一覧（status：Draft/Reviewing/Published/Hold）
  - 記事の再生成、手動公開/非公開（緊急時）
- **Review Logs**
  - レビュー結果（カテゴリスコア、Fail理由）
  - どのソースが原因かの分析（ソース別集計）
- **Templates（記事テンプレ）**
  - テンプレ編集、バージョン発行、ロールバック
- **Prompts（LLMプロンプト管理）**
  - Writer/Reviewer/Revise 各プロンプトの編集
  - バージョン管理/ロールバック
  - テスト実行（サンプル記事でレビューを試す）
- **Donation Campaigns（寄付キャンペーン管理）**
  - キャンペーンCRUD（provider、URL、通貨、合計金額の取得方式）
  - 合計金額の取得ログ、パース失敗時の確認
- **YouTube Cards**
  - カードCRUD
- **History Editor**
  - 年表エントリCRUD、説明文編集

### 6.3 認証・権限
- 管理者1名（MVP）
- 管理画面は認証必須（メール+PW）
- 監査ログ必須（誰がいつ何を変えたか）

---

## 7. ホワイトリスト（情報源）仕様

### 7.1 初期ホワイトリスト（v0）
- UNHCR
- UN Crisis Relief（Venezuela Humanitarian Fund 等）
- R4V
- UNICEF
- WFP
- MSF

### 7.2 取得方式
- RSS優先
- RSSが無い場合のみ低頻度スクレイピング（取得頻度は日次程度）
- 倫理配慮
  - robots.txt尊重
  - ETag/Last-Modified利用
  - 過剰アクセス回避（低頻度、指数バックオフ）

### 7.3 SNSの扱い
- 原則参照しない
- 例外：公式アカウント（ただしMVPでは原則未使用。採用する場合はホワイトリストに明示）

---

## 8. コンテンツ設計（記事・歴史）

### 8.1 日次記事（Daily）
#### 8.1.1 目的
- 30秒で状況が掴める
- "知りたい"が自然に続く（次の疑問が生まれる）
- 寄付へ迷いなく到達できる

#### 8.1.2 記事テンプレ（差し替え可能にする）
記事はテンプレ定義に沿って生成される。テンプレは将来改善が前提のため、**管理画面で差し替え可能**にする。

- テンプレは **JSON**としてバージョン管理
- 新規記事は最新テンプレで生成
- 過去記事は原則そのまま（再生成ボタンで更新可能）

（テンプレ例：後述の Appendix A 参照）

#### 8.1.3 出典・メタデータ
- Factには必ず出典URL（複数可）
- "数字"は必ず「時点 / 対象期間 / 単位」をセット
- 記事には生成日時、使用テンプレ版本、使用プロンプト版本を保存

### 8.2 歴史ページ（Weekly）
- 年表（年/出来事/説明/出典URL）
- 年表に沿った説明文（中立・事実中心）
- 更新は週次目安（管理画面で手動更新）

---

## 9. AIパイプライン（自動生成→レビュー→公開）

### 9.1 全体フロー
1. **Fetch**：ホワイトリストから情報収集（RSS/スクレイプ）
2. **Extract**：本文・見出し・発行日・数値等を抽出
3. **Normalize**：要点・数字・日時・カテゴリを構造化
4. **Write**：テンプレに沿って記事生成
5. **Review**：ルール + LLMでレビュー（カテゴリ別スコア）
6. **Revise**：自動改修（最大2回）
7. **Publish/Hold**：合格→公開、不合格→保留
8. **Log**：全ログ保存（監査・改善・ソース除外判断に使用）

### 9.2 自動改修ルール
- 自動改修は **最大2回**
- 2回不合格の場合：
  - 該当記事は **Hold**
  - 原因ソースをログから特定し、ホワイトリスト除外を検討
  - 次回以降、ソース選択ロジックを変更（より信頼度高へ寄せる）

---

## 10. レビュー仕様（品質ゲート）

### 10.1 レビュー観点（カテゴリ別スコア）
- **Evidence**：出典充足（Factに出典があるか）
- **Neutrality**：政治的意見/評価語の混入
- **Overclaim**：断定/誇張/因果の飛躍
- **Dignity**：尊厳（差別/侮蔑/過度な悲惨演出）
- **Scope**：扱わない話題（外交/制裁/政権評価等）の混入

### 10.2 即Fail条件（例）
- 出典なしの断定（Fact扱い）が存在
- 政治的評価語・断罪表現が存在
- 扱わない話題が主題として含まれる
- 差別/侮蔑/個人特定の危険がある

### 10.3 ログ要件（必須）
- article_id、対象ソース、レビュー結果（カテゴリスコア、Fail理由）
- 自動改修の差分（Before/After）
- 採用テンプレ版本、採用プロンプト版本
- Fail原因の集計（ソース別/パターン別）

---

## 11. 寄付導線とKGI（寄付金額）

### 11.1 方針：プラットフォームは集金しない
- 本システムは資金を保持しない
- 寄付は外部（団体/寄付基盤）で完結

### 11.2 寄付キャンペーン（DonationCampaign）
- 掲載対象：寄付合計金額が取得できるキャンペーンのみ（方式A/B）
- 目的別カテゴリ（例）
  - 難民/移民、子ども、食料、医療、包括ファンド など

### 11.3 KGI算出
- KGI（累計寄付金額） = 掲載中キャンペーンの **合計金額の総和**
- 日次で取得し、DBに保存（履歴を持つ）
- 取得失敗時：
  - 前回値を維持し、エラーを管理画面に表示
  - 一定回数失敗したキャンペーンは "要対応" にする

### 11.4 クリック計測（補助KPI）
- /go/{campaign_id} のリダイレクトでクリック数も保存（補助KPI）
- クリック数はKGIではないが、導線改善の重要な指標として扱う

---

## 12. データモデル（最小構成）

### 12.1 主テーブル案（MVP）
- `sources`：ホワイトリスト
- `fetch_runs`：収集実行ログ
- `raw_items`：取得した記事・ページの生データ（最小でOK）
- `articles`：公開/保留含む記事
- `article_blocks`：Fact/Inference/Unverified のブロック単位（色分け表示のため）
- `review_runs`：レビュー結果と指摘
- `template_versions`：記事テンプレ定義（JSON）
- `prompt_versions`：プロンプト定義（Writer/Reviewer/Revise）
- `donation_campaigns`：寄付キャンペーン（取得方式・URL）
- `donation_snapshots`：キャンペーン金額の時系列（KGI履歴）
- `click_events`：/go クリック
- `youtube_cards`：YouTubeカード
- `history_entries`：年表エントリ

---

## 13. API（概要）

### 13.1 Public
- `GET /api/public/home`：KGI、最新記事サマリ
- `GET /api/public/articles?from=YYYY-MM-DD&to=...`
- `GET /api/public/articles/{id}`
- `GET /api/public/history`
- `GET /go/{campaign_id}`：302リダイレクト + click log

### 13.2 Admin（認証必須）
- `CRUD /api/admin/sources`
- `POST /api/admin/pipeline/run`（手動実行）
- `CRUD /api/admin/articles`（公開/保留/再生成など）
- `GET  /api/admin/review-logs`
- `CRUD /api/admin/templates`
- `CRUD /api/admin/prompts`
- `CRUD /api/admin/donation-campaigns`
- `POST /api/admin/donation-campaigns/{id}/check`（手動取得テスト）
- `CRUD /api/admin/youtube`
- `CRUD /api/admin/history`

---

## 14. ジョブ/スケジュール（Worker）

### 14.1 Daily Pipeline（記事生成）
- 毎日1回（運用で時間設定）
- 実行内容：Fetch → Extract → Write → Review → Revise(最大2) → Publish/Hold → Log

### 14.2 Donation KGI Fetch（寄付金額取得）
- 毎日1回（Daily Pipelineと分離推奨）
- 実行内容：各DonationCampaignの合計金額を取得 → donation_snapshots保存 → KGI更新

### 14.3 Weekly History（手動更新前提）
- 週1回、管理画面に "更新リマインド" 表示（通知は任意）

---

## 15. セキュリティ・コンプライアンス・著作権配慮

### 15.1 管理画面
- 認証必須
- 監査ログ必須（プロンプト/テンプレ/ソース/キャンペーン変更）

### 15.2 外部リンク制御
- 外部リンクはホワイトリスト（sources / donation_campaigns / youtube_cards）に登録済みのみ許可
- ユーザー入力による任意URLは不可（改ざん防止）

### 15.3 著作権配慮
- "転載"を避け、要約中心
- 引用が必要な場合は最小限にし、出典明示と引用要件を満たす（運用ポリシーに明記）

### 15.4 免責・訂正
- AI生成の限界、誤りがあり得ること、訂正方針を明記
- 誤り報告窓口（フォーム/メール）を用意（MVPは簡易でも可）

---

## 16. テスト方針（MVP）

### 16.1 回帰テスト（レビューFailケース）
- 評価語混入 → Fail
- 出典なし断定 → Fail
- 扱わない話題混入 → Fail
- 差別語/侮蔑語 → Fail
- 2回自動改修後もFail → Hold

### 16.2 E2E
- Daily Pipeline：収集→生成→公開までの正常系
- Donation KGI Fetch：金額取得→KGI反映
- /go リダイレクト：クリック記録が残る

### 16.3 運用テスト
- プロンプト/テンプレを更新→ロールバックできる
- ホワイトリスト停止→以後の収集に反映される

---

## 17. OSS運用（Community Drive）

### 17.1 Open Source
- GitHub `somos-venezuela` にてコード公開
- 小さく始めて長く育てる（継続性優先）

### 17.2 Prompt as Code（プロンプト貢献）
- プロンプトは独立ファイルとして管理
- PRで改善できる仕組み（レビューで安全性チェック）
- 管理画面からの変更もバージョン管理し、差分・ロールバック可能にする

### 17.3 Contributionの種類
- Donation：寄付
- Development：機能改善
- Prompt Engineering：読者の理解と関心をつなぐ言葉の改善
- Awareness：周囲へ伝える（煽りではなく理解を促す形で）

---

## Appendix A：テンプレJSON例（Daily v1）
（テンプレはこの形式でDB/ファイルに保存し、管理画面で更新可能にする）

```json
{
  "template_id": "daily_v1",
  "sections": [
    {
      "section_id": "headline_3points",
      "title": "今日わかったこと（3点）",
      "prompt_hint": "30秒で理解できる要点を3つ。政治評価語は禁止。Fact/推測/未確認を明示。",
      "required": true
    },
    {
      "section_id": "impact_life",
      "title": "生活への影響（事実中心）",
      "prompt_hint": "医療/食料/教育/移動など生活項目で整理。Factには出典必須。",
      "required": true
    },
    {
      "section_id": "numbers",
      "title": "数字で見る（時点つき）",
      "prompt_hint": "数字は時点/対象期間/単位/出典URLをセットで提示。",
      "required": false
    },
    {
      "section_id": "unknowns",
      "title": "未確認・わからないこと",
      "prompt_hint": "推測で埋めず、Unverifiedとして理由を添える。",
      "required": true
    },
    {
      "section_id": "donate",
      "title": "支援する（寄付）",
      "prompt_hint": "目的別に寄付キャンペーンを提示。過剰な煽りは禁止。",
      "required": true
    }
  ]
}
```

## Appendix B：DonationCampaign金額取得の仕様メモ（実装ヒント）

- `donation_campaigns.fetch_method`：
  - `PUBLIC_TOTAL_SCRAPE`（ページの公開合計をスクレイプ）
  - `API`（APIで取得）

- スクレイプの場合、キャンペーンごとに
  - 取得URL
  - 通貨
  - パース設定（CSS selector / regex）
  - 小数・カンマ・通貨記号の正規化ルール
を管理画面で設定できるようにする（"取得できる＝KGI要件を満たす"を担保）。

---
### 追加メモ（運用で必ず役立つ）
- 取得失敗時は `last_success_snapshot` を維持し、`last_error` と `error_count` を更新してAdminに表示する
- パースの前処理として、取得HTMLから不要要素（script/style）を落としてから正規表現を当てると事故が減る
- 通貨表記が複数混在する場合に備え、`currency` は「表示用」と「正規化用」を分けてもよい
